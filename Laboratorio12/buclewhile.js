let numero = 0;
while(numero >= 0) {
    numero = parseFloat(prompt("Ingresa un número: "));
}

// Si el el contador se inicializa con un valor negativo
// el bucle no corre, porque evalua la condición como falsa.